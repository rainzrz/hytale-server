# Hytale Server Configuration

# JVM Options (memory allocation)
# Minimum 4GB recommended
JAVA_OPTS=-Xms2G -Xmx4G

# Server port (UDP/QUIC)
SERVER_PORT=5520

# Use Ahead-of-Time cache for faster startup
USE_AOT_CACHE=true

# Extra server arguments (optional)
# Examples:
#   --disable-sentry          Disable crash reporting
#   --backup                  Enable automatic backups
#   --backup-frequency 30     Backup every 30 minutes
#   --auth-mode offline       Offline mode (no authentication)
EXTRA_ARGS=
